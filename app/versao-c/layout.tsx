export const metadata = {
  title: 'Versão C | Última edição do Desafio Perícia Médica Lucrativa',
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
